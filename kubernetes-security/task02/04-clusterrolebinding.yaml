apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: prometheus-pods-reader
subjects:
- kind: Group
  name: system:serviceaccounts:prometheus
  namespace: prometheus
roleRef:
  kind: ClusterRole 
  name: pods-reader
  apiGroup: rbac.authorization.k8s.io